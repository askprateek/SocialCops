import React from "react";
import ReactDOM from "react-dom";
import Layout from './lib/Layout'
import * as ReactD3 from 'react-d3-components';
const app_root = document.getElementById('root');

ReactDOM.render(<Layout />, app_root);
